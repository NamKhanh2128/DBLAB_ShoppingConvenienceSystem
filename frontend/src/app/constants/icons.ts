/**
 * =============================================
 * 🎨 ICON STANDARDS - Single Source of Truth
 * =============================================
 * All icons used in the app should be imported from here.
 * This ensures visual consistency across all pages and components.
 *
 * Standard based on QuickActionModal icon set.
 */

// ─── Domain / Feature Icons ───────────────────────────────────────────────────
export {
  ShoppingCart,   // Mua sắm / Shopping
  Package,        // Kho hàng / Inventory
  Utensils,       // Bữa ăn / Meal Plan
  ChefHat,        // Công thức / Recipes
  FileText,       // Báo cáo / Reports
  Users,          // Gia đình / Family Members
  Home,           // Dashboard / Trang chủ
  Settings,       // Cài đặt / Settings
} from "lucide-react";

// ─── Action Icons ─────────────────────────────────────────────────────────────
export {
  Plus,           // Thêm mới
  Pencil,         // Chỉnh sửa (inline, small)
  Edit,           // Chỉnh sửa (button)
  Trash2,         // Xóa
  Eye,            // Xem chi tiết
  Download,       // Tải xuống / Xuất
  Upload,         // Tải lên / Nhập
  Share2,         // Chia sẻ
  Copy,           // Sao chép
  Check,          // Xác nhận / Tick
  X,              // Đóng / Hủy
  Search,         // Tìm kiếm
  Filter,         // Lọc
  ArrowRight,     // Chuyển sang / Điều hướng
  ChevronRight,   // Mũi tên phải (navigation)
  ChevronLeft,    // Mũi tên trái (navigation)
  Menu,           // Menu / Hamburger
  MoreVertical,   // 3 chấm dọc (context menu)
  LogOut,         // Đăng xuất
} from "lucide-react";

// ─── Status / Indicator Icons ─────────────────────────────────────────────────
export {
  CheckCircle2,   // Thành công / Done
  AlertCircle,    // Lỗi / Error
  AlertTriangle,  // Cảnh báo / Warning
  Info,           // Thông tin
  Star,           // Yêu thích / Rating
  Heart,          // Thích
  Bell,           // Thông báo
  Badge,          // Huy hiệu (note: Badge from ui/badge for component)
} from "lucide-react";

// ─── Data / Time Icons ────────────────────────────────────────────────────────
export {
  Calendar,       // Ngày tháng chung
  CalendarDays,   // Sidebar - Meal Plan nav
  Clock,          // Thời gian / Giờ
  TrendingUp,     // Xu hướng tăng
  TrendingDown,   // Xu hướng giảm
  BarChart3,      // Biểu đồ cột (sidebar Reports nav)
  DollarSign,     // Tiền tệ / Chi phí
} from "lucide-react";

// ─── User / Auth Icons ────────────────────────────────────────────────────────
export {
  User,           // Người dùng đơn
  UserCircle,     // Avatar người dùng
  UserPlus,       // Mời / Thêm thành viên
  Mail,           // Email
  Phone,          // Điện thoại
  Lock,           // Mật khẩu / Bảo mật
  Eye as EyeShow, // Hiển thị mật khẩu
  EyeOff,         // Ẩn mật khẩu
  Shield,         // Quyền hạn / Admin
} from "lucide-react";

// ─── Misc / Decoration Icons ──────────────────────────────────────────────────
export {
  Sparkles,       // Logo / AI / Magic
  MapPin,         // Vị trí
  Tag,            // Danh mục / Label
  Hash,           // Số lượng
  Link,           // Liên kết URL
  Globe,          // Ngôn ngữ / Quốc tế
  Database,       // Database / Master data (Admin)
  Activity,       // Hoạt động
  Layers,         // Phân loại / Nhóm
  MessageCircle,  // Tin nhắn
  Printer,        // In ấn
  Save,           // Lưu
  Loader2,        // Loading spinner
  ZoomIn,         // Phóng to
} from "lucide-react";
